# Choreography And Handback

## With The Other Round-2 Requests

- **hypervisor** (`phase4-oom-fix-and-capture-engine-proving/`): their
  item 4 answers `9bx`; our item 3 consumes it. Their capture-engine
  proof (item 5) is the engine-side sibling of our route-side smoke —
  different layers, both cited by refwork's corpus work.
- **reference-workload** (`phase4-real-capture-corpus-fast-follow/`):
  our smoke is the proof behind their "second capture route" option;
  the shared operator window (their lab session + our eqb + this
  smoke) is the efficiency play — propose it when their round-1
  schedules.
- **snapshot-store** (`phase2-closeout-m8-joint-fork-integrity/`):
  their joint session shares the same production box; we own the
  restart windows (`72o`) — one calendar, we broker it.

## Phases-Track Verification

1. Rider: we check round-1's eventual eqb record against the rider's
   bar (reopens counted, variables recorded, spot-check present).
2. Smoke: we verify the sanitized record's internal consistency, that
   the redaction gate ran (no private material in git), that the
   capture id resolves against `captures/index.jsonl`, and that `r77`
   is closed (or its named residual recorded) with the record cited at
   both named locations.
3. `9bx`: the delta addendum exists if and only if the budget changed.

## Handback Shape

The rider itself is this request's first deliverable — note its path
here when written. Full resolution (`04-resolution.md`) after the
gated items: smoke record location, bead dispositions, delta addendum
if any. We respond with `05-verification.md`.

## Contact / Tracking

- Beads: `l1w`, `9bx` (this request); `eqb` (round-1, amended by the
  rider); the deferred `r77`/`opw`/`0wo` chain (context, not scope).
- Predecessors: round-1 (this repo), hypervisor round-2, refwork
  round-1/round-2.
